[project]
name = "cdk"
version = "0.1.0"
description = "CDK infrastructure for the candidate search service"
requires-python = ">=3.14"
dependencies = [
    "aws-cdk-lib>=2.219.0",
    "constructs>=10.3.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
