[project]
name = "common-layer"
version = "0.1.0"
description = "Shared runtime dependencies for the candidate search Lambdas"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.20.0",
    "psycopg[binary]>=3.3.5",
    "sqlalchemy>=2.0.52",
]
